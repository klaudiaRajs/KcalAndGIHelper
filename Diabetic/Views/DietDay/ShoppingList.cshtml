@model List<IngredientDto>

<h1>Shopping List</h1>

<table class="table table-dark">
    <thead>
        <tr>
            <th scope="col">Ingredient name</th>
            <th scope="col">Amount</th>
        </tr>
    </thead>
    <tbody>
        @foreach (IngredientDto item in Model)
        {
            <tr>
                <td>@item.Product.Name</td>
                <td>@item.Amount</td>
            </tr>
        }
    </tbody>
</table>